class Solution:
    def generate(self, numRows: int) -> List[List[int]]:
        n=numRows
        res=[]
        for i in range(n):
            num=[1]*(i+1)
            for j in range(1,i):
                num[j]=res[i-1][j-1]+res[i-1][j]
            res.append(num)
        return res
